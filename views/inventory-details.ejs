<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inventory Details</title>
  <!-- Add any additional styles or links to external stylesheets here -->
  <link rel="stylesheet" href="/styles/home.css">
  <link rel="stylesheet" href="/styles/inventory-details.css">
</head>
<body>
  <header>
    <h1>Inventory Details</h1>
    <a href="/logout">Logout</a>
  </header>
  <div class="container">
    <% if (item) { %>
      <h2><%= item.name %></h2>
      <p><strong>Item ID:</strong> <%= item.restaurantID %></p>
      <p><strong>Cuisine:</strong> <%= item.cuisine %></p>
      <p><strong>Phone:</strong> <%= item.phone %></p>
      <p><strong>Description:</strong> <%= item.description %></p>
      <p><strong>Address:</strong> <%= item.address ? `${item.address.street}, ${item.address.borough}` : 'N/A' %></p>
      <a href="/edit?_id=<%= item._id %>">Edit</a>
      <a href="/delete?_id=<%= item._id %>">Delete</a>
    <% } else { %>
      <p>No details found for this item.</p>
    <% } %>
  </div>
  <!-- Add any additional HTML elements or links here -->
</body>
</html>
